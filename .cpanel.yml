---
deployment:
  tasks:
    - export DEPLOYPATH=/home/u2255410/public_html/
    - /bin/cp -R .next/standalone $DEPLOYPATH
    - /bin/cp -R .next/static $DEPLOYPATH/.next/static
    - /bin/cp -R public $DEPLOYPATH
    - /bin/mkdir -p $DEPLOYPATH/src
    - /bin/cp -R src/app $DEPLOYPATH/src
    - /bin/cp package.json $DEPLOYPATH
    - /bin/cp next.config.ts $DEPLOYPATH
    - /bin/cp tailwind.config.js $DEPLOYPATH
    - /bin/cp postcss.config.mjs $DEPLOYPATH
    - /usr/local/bin/npm install --production
